<div>
  {{#if user}}
    <section>
      {{#each user.posts}}
        <article>
          <h2>{{title}}</h2>
          <p>{{body}}</p>
        </article>
      {{/each}}
    </section>
  {{/if}}
</div>
